apiVersion: v1
kind: Service
metadata:
  name: {{ include "istone.fullname" . }}
  labels:
    {{- include "istone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "istone.selectorLabels" . | nindent 4 }}
